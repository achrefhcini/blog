# BLOG TEST Laravel + VueJs 

### Installation



```sh
$ cd blog
$ composer install 
$ npm install 
```

Configurer la connexion de la base de donnée dans .env

Lancer le projet
```sh
$ npm run dev
$ php artisan migrate:fresh --seed
$ php artisan serve
```







